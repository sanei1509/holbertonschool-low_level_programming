#include "main.h"

/**
*main -write program that prints his name, followed by a new line
*@argc: count the quantity of arguments
*@argv: different data in a vector
*Return: 0
*/

int main(int __attribute__((unused)) argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
