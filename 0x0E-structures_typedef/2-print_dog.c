#include <stdio.h>
#include "main.h"

/**
*print_dog - print a struct dog
*@struct dog: receive a structure of dog
*@d: receive a pointer
*/

void print_dog(struct dog *d)
{
	if (d => name)
	{
		printf(nil);
	}
	if(d => name == NULL)
}
